with newtable as(
select artist_name,avg(danceability) as 'ArtistAvg',(select avg(danceability) from tracks_features ) as 'TotalAvg' from tracks_features
group by artist_name )
select artist_name,(TotalAvg-ArtistAvg) as AboveAvg from newtable 
order by AboveAvg desc 
